<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--uncomment to enable specific log level for a class-->
    <!--<logger name="com.consideredgames.game.model.hex.RiverNetwork" level="DEBUG"/>-->
    <!--<logger name="com.consideredgames.game.model.hex.FlowInitialiser2" level="DEBUG"/>-->

    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>